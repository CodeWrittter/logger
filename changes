git commit -m "$(cat <<'EOF'

Full project: SQLite DB (10 tables), 10 logging receivers/workers,
Supabase sync, PIN lock screen, and all 5 UI tabs (Trigger, Sync,
Logging, Data, App) with permission management and device admin.

EOF
)"
